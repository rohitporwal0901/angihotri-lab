import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agnihotri.lab',
  appName: 'AgnihotriLab',
  webDir: 'dist/lab-booking-app/browser'
};

export default config;
